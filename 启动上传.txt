cd UndergroundCityEngineering


------------------------------------------------------

git add .
git commit -m "添加 多个文件"
git push -u origin Bean_Flame

------------------------------------------------------


git add .
git commit -m "添加 多个文件"
git push -u origin master

------------------------------------------------------

echo “push origin master 执行完成”
pause

------------------------------------------------------
创建并切换分支

git checkout -b master origin/master 
git checkout -b Bean_Flame 
------------------------------------------------------

git checkout -b master

git checkout -b master